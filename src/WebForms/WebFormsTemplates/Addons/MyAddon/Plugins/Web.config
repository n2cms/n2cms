<?xml version="1.0"?>

<configuration>
  <system.web>
    <!-- Restrict access to editors -->
    <authorization>
      <allow users="Admin" roles="Administrators,Editors"/>
      <deny users="*"/>
    </authorization>
  </system.web>
</configuration>
