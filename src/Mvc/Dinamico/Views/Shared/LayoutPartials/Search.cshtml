@{using (Html.BeginForm("Search", null, new { id = "search", item = Content.Traverse.StartPage }, FormMethod.Get, new { id = "searchform" }))
  {
	<input name="q" value="Search"/>
	<button type="submit"></button>
  }
}
