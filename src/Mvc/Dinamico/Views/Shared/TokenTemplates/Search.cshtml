@model string

@{ var result = Content.Services.Resolve<N2.Persistence.Search.ITextSearcher>().Search(N2.Persistence.Search.SearchQuery.For(Model).Below(Content.Traverse.StartPage).Pages(true)); }
@Html.UnorderedList(result.Hits, @<span>@Content.LinkTo(item.Data.Content)</span>)
