Views provided by the dinamico templates are located in /Themes/Default/Views/ 
and are served by the ThemeViewEngine. When a view exists in a the currently
selected theme location /Themes/[SelectedThemeName]/Views/ this is picked 
before the default view. The same applies for styles in the content folder.