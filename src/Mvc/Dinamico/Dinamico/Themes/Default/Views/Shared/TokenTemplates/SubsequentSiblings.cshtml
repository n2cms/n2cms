@model string

@using (Content.BeginScope(Model))
{
	@Html.UnorderedList(Content.Traverse.Siblings(Content.Is.AccessiblePage()).SkipWhile(s => s != Content.Path.CurrentItem).Skip(1), @<span>@Content.LinkTo(item.Data)</span>)
}
