@model string

@using (Content.BeginScope(Model))
{
	@Html.UnorderedList(Content.Traverse.Siblings(Content.Is.AccessiblePage()).TakeWhile(s => s != Content.Path.CurrentItem), @<span>@Content.LinkTo(item.Data)</span>)
}
