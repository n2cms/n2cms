@model string
@using (Content.BeginScope(Model))
{
	var next = @Content.Traverse.Children(Content.CurrentItem.Parent, Content.Is.AccessiblePage()).SkipWhile(i => i != Content.CurrentItem).Skip(1).FirstOrDefault();
	@Content.LinkTo(next)
}	