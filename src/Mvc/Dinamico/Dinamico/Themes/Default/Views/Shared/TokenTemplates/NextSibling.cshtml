@model string
@using (Content.BeginScope(Model))
{
	var next = @Content.Traverse.Children(Content.Traverse.Parent(), Content.Is.AccessiblePage()).SkipWhile(i => i != Content.Path.CurrentItem).Skip(1).FirstOrDefault();
	@Content.LinkTo(next)
}	